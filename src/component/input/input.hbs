<input class="{{classNameInput}}"
type="{{typeInput}}"
id={{ idInput }}
  {{#if valueInput }}
  value={{ valueInput }}
  {{/if}}
  name="{{nameInput}}"
  placeholder={{ placeholderInput }}
  required></input>
